import { Flex } from "@chakra-ui/react";

export const Landing = () => {

  // need a component to sign and prove ownership of a message
  // need a component to verify a message was signed by a specific address
  return (
    <Flex flexDirection="column" justifyContent="center" alignItems="center" h="calc(100% - 6rem)" gap={8}>
     {/*  */}
    </Flex>
  );
}